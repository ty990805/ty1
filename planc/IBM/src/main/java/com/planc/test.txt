SWA(ABOVE)
LIMMSG(SYSTEM)
CTRACE(CTIBPXST)
KERNELSTACKS(ABOVE)
FILESYSTYPE TYPE(NFS) ENTRYPOINT(GFSCINIT) ASNAME(MVSNFSCL)
               PARM('AttrCaching(Y)')
FILESYSTYPE TYPE(TFS1) ENTRYPOINT(BPXTFS) ASNAME(OOKASPT1,'SUB=MSTR')
ROOT FILESYSTEM('&OMVSHLQ..SVT.SYSPLEX.ZFS')
   TYPE(ZFS) MODE(RDWR)
     MKDIR('smbdrivh')
     MKDIR('le')
     MKDIR('JDaemon')
     MKDIR('avtgen')
     MKDIR('home')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..SYSTEM.ZFS')
   TYPE(ZFS) MODE(RDWR)
   UNMOUNT
   MOUNTPOINT('/&SYSNAME')
   MKDIR('sshtest')
   MKDIR('IBWJ')
   MKDIR('zWebSpheredmgr61')
   MKDIR('WebSphere/V6R1')
   MKDIR('WebSphere/V6R1/C1CELL')
   MKDIR('WebSphere/V6R1/C1CELL/C1NODE&SYSID1.')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&RLSE..ROOT.ZFS')
   TYPE(ZFS) MODE(RDWR)
   AUTOMOVE(INCLUDE,J40,J60,D0,D4,N64,N66,NP0,NP2,*)
   MOUNTPOINT('/$VERSION')
   MKDIR('opt/Tivoli')
   MKDIR('opt/Tivoli/RM')
   MKDIR('usr/lpp/zosmf/V2R1_SPE1')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..DEV.TFS')
   PARM('-c 10 -s 100000 -fsfull(50,5) -ea 100   -em 100 ')
   TYPE(TFS1) MODE(RDWR) UNMOUNT MOUNTPOINT('/&SYSNAME/dev')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..ETC.ZFS')
   TYPE(ZFS) MODE(RDWR) UNMOUNT
   MOUNTPOINT('/&SYSNAME/etc')
   MKDIR('ssh')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..TMP.TFS')
   PARM('-c 10 -s 1000000') TYPE(TFS1) MODE(RDWR) UNMOUNT
   MOUNTPOINT('/&SYSNAME/tmp')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..VAR.ZFS')
   TYPE(ZFS) MODE(RDWR) UNMOUNT
   MOUNTPOINT('/&SYSNAME/var')
   MKDIR('Tivoli')
   MKDIR('Tivoli/RM')
   MKDIR('opt')
   MKDIR('opt/Tivoli')
   MKDIR('opt/Tivoli/RM')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..SSH.ZFS')
   TYPE(ZFS) MODE(RDWR) UNMOUNT
   MOUNTPOINT('/&SYSNAME/etc/ssh')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..SSHTEST.ZFS')
   TYPE(ZFS) MODE(RDWR) UNMOUNT
   MOUNTPOINT('/&SYSNAME/sshtest')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..DIRHFS.HFS')
   TYPE(HFS) MODE(RDWR) UNMOUNT
   MOUNTPOINT('/&SYSNAME/sshtest/sshatter/dirhfs')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.LOCAL.ZFS')
   TYPE(ZFS) MODE(RDWR)
   MOUNTPOINT('/local')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.U.ZFS')
   TYPE(ZFS) MODE(RDWR)
   MOUNTPOINT('/u')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.UPERF.ZFS')
   TYPE(ZFS) MODE(RDWR)
   MOUNTPOINT('/uperf')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.ICT.ZFS')
   TYPE(ZFS) MODE(RDWR)
   MOUNTPOINT('/ict')
   MKDIR ('xml')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.ICTROFS.ZFS')
   TYPE(ZFS) MODE(READ)
   AUTOMOVE(INCLUDE,J40,J60,D0,D4,N64,N66,NP0,NP2,*)
   MOUNTPOINT('/ict/rofs')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.ICTRWFS.ZFS')
   AUTOMOVE(INCLUDE,J40,J60,D0,D4,N64,N66,NP0,NP2,*)
   TYPE(ZFS) MODE(RDWR)
   MOUNTPOINT('/ict/rwfs')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.ICTBIN64.ZFS')
   TYPE(ZFS) MODE(RDWR)
   AUTOMOVE(INCLUDE,J40,J60,D0,D4,N64,N66,NP0,NP2,*)
   MOUNTPOINT('/ict/bin')
        PARM('NOREADAHEAD,FSFULL(50,5)')
MOUNT FILESYSTEM('&OMVSHLQ..NOSETUID.ZFS')
   TYPE(ZFS) MODE(RDWR)
   MOUNTPOINT('/ict/isaqctlnosuidfs')  NOSETUID
MOUNT FILESYSTEM('&OMVSHLQ..SVT.JAVA.ZFS')
   TYPE(ZFS) MODE(RDWR)
   MOUNTPOINT('/java')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.PRDJAVA1.ZFS')
   TYPE(ZFS) MODE(RDWR) AUTOMOVE
   MOUNTPOINT('/prodjava1')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.PRDJAVA2.ZFS')
   TYPE(ZFS) MODE(RDWR) AUTOMOVE
   MOUNTPOINT('/prodjava2')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.JAVATEST.ZFS')
   TYPE(ZFS) MODE(RDWR) AUTOMOVE
   MOUNTPOINT('/javatest')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.JAVAOUT.ZFS')
   TYPE(ZFS) MODE(RDWR) AUTOMOVE
   MOUNTPOINT('/javaoutm')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.&SYSNUMB..JAVAOUT.ZFS')
   TYPE(ZFS) MODE(RDWR) UNMOUNT
   MOUNTPOINT('/&SYSNAME/javaout')
MOUNT FILESYSTEM('&OMVSHLQ..SVT.JAVALP.ZFS')
    MOUNTPOINT('/svt/java/wkld/javaLP') TYPE(ZFS) MODE(RDWR)
    AUTOMOVE
MOUNT FILESYSTEM('&OMVSHLQ..JAVA.J531.ZFS')
    MOUNTPOINT('/java/builds/J5.0') TYPE(ZFS) MODE(READ)
    AUTOMOVE
MOUNT FILESYSTEM('&OMVSHLQ..JAVA.J564.ZFS')
    MOUNTPOINT('/java/builds/J5.0_64') TYPE(ZFS) MODE(READ)
    AUTOMOVE
MOUNT FILESYSTEM('&OMVSHLQ..JAVA.J631.ZFS')
   MOUNTPOINT('/java/builds/J6.0') TYPE(ZFS) MODE(READ)
   AUTOMOVE
MOUNT FILESYSTEM('&OMVSHLQ..JAVA.J664.ZFS')
    MOUNTPOINT('/java/builds/J6.0_64') TYPE(ZFS) MODE(READ)
    AUTOMOVE
MOUNT FILESYSTEM('&OMVSHLQ..JAVA.J7031.ZFS')
    MOUNTPOINT('/java/builds/J7.0') TYPE(ZFS) MODE(READ)
    AUTOMOVE
MOUNT FILESYSTEM('&OMVSHLQ..JAVA.J7064.ZFS')
    MOUNTPOINT('/java/builds/J7.0_64') TYPE(ZFS) MODE(READ)
    AUTOMOVE
MOUNT FILESYSTEM('&OMVSHLQ..JAVA.J7131.ZFS')
    MOUNTPOINT('/java/builds/J7.1') TYPE(ZFS) MODE(READ)
    AUTOMOVE
MOUNT FILESYSTEM('&OMVSHLQ..JAVA.J7164.ZFS')
    MOUNTPOINT('/java/builds/J7.1_64') TYPE(ZFS) MODE(READ)
    AUTOMOVE
MOUNT FILESYSTEM('ZOSJAVASVT') TYPE(NFS) MODE(RDWR)
    MOUNTPOINT('/java/remote')
TRANSREC NODESMF((*,*))
         CIPHER(YES)
         OUTWARN(10000,5000)
         OUTLIM(100000)
         VIO(SYSDA)
         LOGSEL(LOG)
         LOGNAME(MISC)
         DAPREFIX(TUPREFIX)
         USRCTL(NAMES.TEXT)
         SYSOUT(*)
SEND
   OPERSEND(ON)
   USERSEND(ON)
   SAVE(ON)
   CHKBROD(ON)
   USEBROD(ON)
   MSGPROTECT(OFF)
   LOGNAME(BRODCAST)
